<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="using:NssOrderTool"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="500"
        x:Class="NssOrderTool.MainWindow"
        Title="NSS 順序推定ツール (C#)"
        Width="800" Height="550"
        WindowStartupLocation="CenterScreen">

	<TabControl Margin="5">

		<TabItem Header="メイン" FontSize="16" Height="40">

			<Grid Margin="10" ColumnDefinitions="*, 20, *">

				<StackPanel Grid.Column="0" Spacing="10">
					<StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
						<TextBlock Text="✏️ 入力" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
						<Border Name="EnvBadge" CornerRadius="4" Padding="8,2" Background="Gray">
							<TextBlock Name="EnvText" Text="LOADING" Foreground="White" FontWeight="Bold" FontSize="12"/>
						</Border>
					</StackPanel>

					<TextBlock Text="プレイヤー名をカンマ区切りで入力 (例: A, B, C)" FontSize="12" Foreground="Gray"/>

					<TextBox Name="InputBox"
                             Watermark="例: Player1, Player2, Player3"
                             Height="100"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"/>

					<Button Name="RegisterButton"
                            Content="データを登録"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Center"
                            Background="#007ACC" Foreground="White"
                            Click="RegisterButton_Click"/>

					<TextBlock Name="StatusText"
                               Text="準備完了"
                               Foreground="Green"
                               TextWrapping="Wrap"/>
				</StackPanel>

				<StackPanel Grid.Column="2" Spacing="10">
					<Grid ColumnDefinitions="*, Auto, Auto">
						<TextBlock Grid.Column="0" Text="🏆 推定ランキング" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>

						<Button Grid.Column="1" Name="ReloadButton" Content="更新" Click="ReloadButton_Click" Margin="0,0,5,0"/>

						<Button Grid.Column="2" Name="ClearButton" Content="🗑️ 初期化"
                                Background="#FFCCCC" Foreground="Red"
                                Click="ClearButton_Click"/>
					</Grid>

					<ListBox Name="RankingList" Height="380" Background="#F0F0F0" Foreground="Black">
						<ListBox.ItemTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding}" Padding="5" Foreground="Black"/>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</StackPanel>
			</Grid>
		</TabItem>

		<TabItem Header="設定" FontSize="16">
			<Grid Margin="20" ColumnDefinitions="*, 20, *">

				<StackPanel Grid.Column="0" Spacing="15">
					<TextBlock Text="➕ エイリアス追加" FontWeight="Bold" FontSize="16"/>
					<TextBlock Text="表記揺れを正規の名前に自動変換します。" FontSize="12" Foreground="Gray"/>

					<StackPanel Spacing="5">
						<TextBlock Text="正規名 (変換後の名前)" FontWeight="Bold" FontSize="12"/>
						<TextBox Name="TargetInput" Watermark="例: Takahiro"/>
					</StackPanel>
					
					<StackPanel Spacing="5">
						<TextBlock Text="別名 (入力される名前)" FontWeight="Bold" FontSize="12"/>
						<TextBox Name="AliasInput" Watermark="例: Taka"/>
					</StackPanel>

					<Button Name="AddAliasButton"
                            Content="追加する"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Center"
                            Background="#007ACC" Foreground="White"
                            Click="AddAliasButton_Click"/>

					<TextBlock Name="AliasStatusText"
                               Text=""
                               Foreground="Green"
                               TextWrapping="Wrap"/>
				</StackPanel>

				<StackPanel Grid.Column="2" Spacing="10">
					<Grid ColumnDefinitions="*, Auto">
						<TextBlock Grid.Column="0" Text="📝 登録済みエイリアス" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
						<Button Grid.Column="1" Content="更新" Click="ReloadAliases_Click"/>
					</Grid>

					<ListBox Name="AliasList" Height="380" Background="#F0F0F0" Foreground="Black">
						<ListBox.ItemTemplate>
							<DataTemplate x:DataType="local:AliasGroupItem">
								<Grid ColumnDefinitions="*, Auto, Auto">
									<TextBlock Grid.Column="0"
											   Text="{Binding DisplayText}"
											   VerticalAlignment="Center"
											   Foreground="Black"/>

									<Button Grid.Column="1"
                                            Content="編集"
                                            FontSize="10"
                                            Padding="8,4"
                                            Margin="5,0"
                                            Background="#DDDDDD" Foreground="Black"
                                            Click="EditGroupButton_Click"/>

									<Button Grid.Column="2"
                                            Content="全削除"
                                            FontSize="10"
                                            Padding="8,4"
                                            Background="#FFCCCC" Foreground="Red"
                                            Click="DeleteAliasButton_Click"/>
								</Grid>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</StackPanel>
			</Grid>
		</TabItem>

	</TabControl>
</Window>